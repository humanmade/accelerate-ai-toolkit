{
  "name": "accelerate-ai-toolkit",
  "version": "1.5.0",
  "description": "Conversational access to your Accelerate WordPress site. Ask analytics questions in plain English, get answers backed by real data.",
  "mcpServers": {
    "wordpress": {
      "command": "npx",
      "args": ["-y", "@automattic/mcp-wordpress-remote@latest"],
      "env": {
        "WP_API_URL": "$WP_API_URL",
        "WP_API_USERNAME": "$WP_API_USERNAME",
        "WP_API_PASSWORD": "$WP_API_PASSWORD",
        "OAUTH_ENABLED": "false"
      }
    }
  }
}
