---
description: Connect the Accelerate AI Toolkit to your WordPress site. Walks you through generating an Application Password and saving credentials securely.
---

The user wants to connect the Accelerate AI Toolkit to their WordPress site. Invoke the `accelerate-connect` skill and follow its full setup walkthrough.

Start with the friendly welcome, then step through: site URL → Application Password generation instructions → username and password collection → writing `~/.config/accelerate-ai-toolkit/env` with `chmod 600` → shell profile line → restart reminder.

Don't skip steps. Don't show the full password back to the user after writing it. Use the `accelerate-connect` skill's guidance exactly.
